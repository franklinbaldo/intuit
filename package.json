{
  "name": "intuit",
  "version": "1.0.0",
  "description": "Intuit is a minimalist web tool that decodes and renders HTML content passed as a URL parameter. Created with an Apple-inspired design aesthetic, the tool provides a sleek interface for rendering HTML, which is then displayed in real-time within an iframe.",
  "main": "index.js",
  "scripts": {
    "test": "node --test",
    "lint": "npx eslint . --ext .js,.html",
    "lint:fix": "npx eslint . --ext .js,.html --fix",
    "format": "prettier --write \"**/*.{js,jsx,html,css,json,md}\"",
    "format:check": "prettier --check \"**/*.{js,jsx,html,css,json,md}\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^10.1.5",
    "eslint-plugin-html": "^8.1.3",
    "eslint-plugin-prettier": "^5.5.1",
    "globals": "^16.3.0",
    "prettier": "^3.6.2"
  }
}
